<?php

namespace {{ namespace }};

{{ use }}

class {{ classname }} {{ extends }} {{ implements }}
{
    {{ use_traits }}

    {{ table }}

    {{ primary_key }}

    {{ attributes }}

    {{ relations }}
}
